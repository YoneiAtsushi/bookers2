<h1>Books</h1>

<%= image_tag @user.get_profile_image(100,100) %>

<table>
 
  <tr>
   <th>Title</th>
   <th>Opinion</th>
  </tr>

  <% @books.each do |book| %>
  
  <tr>
   <td><%= book.title %></td>
   <td><%= book.body %></td>
  </tr>
 <% end %>
</table>

 <p><%= link_to "プロフィール編集", edit_user_path(@user.id) %></p>